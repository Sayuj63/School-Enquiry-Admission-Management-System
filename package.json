{
  "name": "sayuj",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel -r run dev",
    "dev:api": "pnpm --filter @sayuj/api dev",
    "dev:web": "pnpm --filter @sayuj/web dev",
    "build": "pnpm -r run build",
    "start": "pnpm --filter @sayuj/api start",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}